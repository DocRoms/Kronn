# cargo-audit configuration — see https://github.com/rustsec/rustsec/tree/main/cargo-audit
#
# Do not add an ignore without a dated rationale and review deadline. CI runs
# `cargo audit` (see `.github/workflows/ci-test.yml`).
[advisories]
ignore = []
