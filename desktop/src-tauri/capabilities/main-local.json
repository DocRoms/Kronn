{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-local",
  "description": "Minimal bootstrap permissions for Kronn's bundled main window.",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-wait-for-backend",
    "allow-restart-app"
  ]
}
