{
  "name": "kronn-desktop",
  "version": "0.9.6",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:sidecar": "node scripts/build-docs-sidecar.mjs",
    "build:frontend": "cd ../frontend && pnpm build"
  }
}
