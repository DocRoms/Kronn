# September 13 important-message qualification

This is the principal's bounded KT-619 acceptance record, not whole-release
approval. The preserved worker commit `0e766c2c` was merged into the existing
qualification checkout, then reviewed and corrected there. No worker history,
host credentials, application database, release version or publication was
changed by this qualification. The cancelled worker checkout remains preserved.
[src: commit: 43f517349c9943eea519303a0bf5743137d40b6]

## Exact source and evidence

The final executable candidate is
`d9879cf201cb2bd9b3eef460fa6cf3780e8b8864`. Its backend tree is
`93da4543c91ba8131e63b1fd68856551a385cfb7`, unchanged since
`2a886d445ac43850493bf389974ce9873de69b11`. Its frontend source tree is
`e2884cc6165c17d4cd9d28a086859cbb53149af2`, unchanged since
`c2173ebafcd7412dfdf24a2b40f17c0b9a69c7e9`. The last commit supplies a
required initial prompt in the new browser fixture; it does not change product
code. Later documentation-only commits do not alter these tested trees.
[src: commit: d9879cf201cb2bd9b3eef460fa6cf3780e8b8864]

Raw local receipts and stdout/stderr are retained under
`/private/tmp/release-013-takeover-proof.XMDShb/`. These are execution artifacts,
not public CI results. Every qualifying wrapper recorded a clean tracked tree,
the expected HEAD before and after execution, no source drift and no timeout.

| Gate | Exact checkpoint and result | Receipt within that directory |
| --- | --- | --- |
| Full Rust, offline and locked, no test filter | `2a886d445ac4`: 7,056 passed, zero failed, six ignored; 28 top-level suites; 792.455 seconds | `2a886d445ac4-full-steering-final.json` |
| Formatting and all-target Clippy with `-D warnings` | `2a886d445ac4`: both pass; Clippy 53.698 seconds | `2a886d445ac4-{fmt,clippy}-steering-final.json` |
| Full frontend | `c2173ebafcd7`: 4,205 passed, zero failed/pending, 319 files; 105.751 seconds | `c2173ebafcd7-front-navigation-final.json` and `-tests.json` |
| Type generation, both installed TypeScript compilers, Oxlint, ESLint, i18n, Vite build | `c2173ebafcd7`: all pass; 686 exports, generated file unchanged | `c2173ebafcd7-{typegen,ts7,ts6,oxlint,eslint,i18n,vite}-navigation-final.json` |
| Isolated Python and shell | `2a886d445ac4`: 809 helper tests plus 14 sidecar tests; 284 shell tests, zero shell skips | `2a886d445ac4-{python,shell}-steering-final.json` |
| Chromium, no retries | `d9879cf201cb`: nine expected, zero unexpected/skipped/flaky; 15.456 seconds | `kt619-browser-1vp60U/receipt.json` and `playwright.json` |

Three exact-filter isolation child runs each passed one test inside the API
suite; they are **not** added to the 7,056 top-level Rust count. The six ignored
Rust cases are retained, not presented as passes. ESLint still reports 63
existing warnings; i18n has 4,563 keys in each of four locales and 726 advisory
warnings. Existing ts-rs, macOS compact-unwind and Vite dependency/chunk notices
also remain. A green gate is not a claim of warning-free output.

## Acceptance and regression controls

The implementation contract and operator limitations are documented in
[important messages](../architecture/important-messages.md) and
[publication authority](../architecture/important-message-publication-authority.md).
The five KT-619 criteria map to the following reviewed behavior:

| Criterion | Evidence and boundary |
| --- | --- |
| Durable, indexed, versioned message with closed categories and bounded fields | Real database/API tests cover validation, replay, deduplication, reopening, server-owned metadata and separate ordinary messages. The ordered registry uses additive migrations 171–174; no migration 175 was added. |
| Controlled Human/Orchestrator publication; worker refusal | Real router/middleware tests exercise public identity impersonation, invite/join non-elevation, explicit grants, possession proof, rollback and revoked/expired/mismatched/replayed Human proofs. Backend steering events create distinct durable rows. Routine completion does not automatically become an accepted-delivery important card. |
| Full-width card, filtering and navigation | Component tests cover category/author/date/action/references, keyboard behavior, server rows without a Markdown fence, bounded selection and per-room position. Returning to a room restores its selected message during the mounted page lifetime; a full page reload resets this UI selection. |
| Nominal and adversarial tests, no relaxed gates | The full suites above include the authority, queue, persistence and navigation regressions. Real-backend browser cases cover enrollment/refusal, posting, replay, restored outbox identity, room navigation and bootstrap rotation. Two additional browser cases exercise the actual card CSS at 360px and 1200px, not the mounted React application. |
| Documentation and tour, exact delivery proof | Architecture/operator documentation and four-locale tour text describe publication rights, explicit credentials, delivery separation and navigation. This record ties acceptance to exact commits and retained receipts, without claiming a release, live provider or operating-system security boundary. |

Principal regression fixes were demonstrated failing before their correction:

- Queued Human publication obtains fresh authority immediately before sending;
  cancellation during preparation cannot issue a late write. Once the write
  begins, it remains tracked. Retry preserves the message UUID, while authority
  material never enters the persisted outbox. Lost-receipt replay cannot
  retroactively promote an ordinary message.
  [src: commit: e49737c74a805032263abc21799392f42da0bf35]
- A real no-agent discussion with an unavailable native provider exposed a
  disabled composer. The corrected guard uses the current room's authoritative
  mode; unknown mode and native-required rooms remain guarded. Component RED
  controls preceded the passing full frontend and real-browser replay.
  [src: commit: 1f168a01f969ff390517b86354e1aad83462dc19]
- Server steering rows were counted but not rendered as cards. Regression tests
  now require the matching durable orchestration source, including the folded
  transcript. A backend RED control also caught routine Done events being
  promoted automatically; the corrected path preserves the ordinary report.
  [src: commit: 2a886d445ac43850493bf389974ce9873de69b11]
- Five initially failing navigation/store tests exposed cross-room selection,
  stale indices after removal/reordering and a lost refresh during an in-flight
  read. Stable message IDs and one coalesced follow-up read fix these cases;
  the added StrictMode test preserves one initial request. The ninth browser
  case verifies third-of-three → other-room first-of-one → third-of-three.
  [src: commit: c2173ebafcd7412dfdf24a2b40f17c0b9a69c7e9]

The first new cross-room browser fixture failed because it omitted the required
initial prompt. That failed run (`kt619-browser-S1AK4d`, seven passed, one failed,
one not run) is retained and is **not** qualifying evidence. The corrected final
fixture passed all nine cases. Earlier failed sandbox/harness runs are likewise
retained rather than relabeled as passes.

## Isolation and remaining release work

The browser used a copied backend binary with SHA-256
`59113941c5aa7331cb06b94dc0bb57a6ab1ac492af9c2699fb585db345d53ebd`,
a fresh disposable database, owned data/host/XDG roots, disabled keychain and
Docs fallback, and two exclusive loopback ports. The four host MCP configuration
hashes and the MAIN listener PID were unchanged within the final run. Only the
harness-owned process groups were stopped afterwards; fixtures were retained.
Neither `HOME` nor `CODEX_HOME` was repurposed.
[src: file: scripts/e2e-sandbox-backend.sh:1]
[src: file: frontend/e2e/fixtures/publication-sandbox.mjs:1]

Server-generated steering is proven by real backend tests plus mounted React
component tests, not by a full browser-driven orchestration execution. No paid
provider, live native adapter, whole-app browser matrix, desktop build, complete
0.12 upgrade or fresh remote CI/coverage run is claimed here. These release-level
criteria remain with KT-543/544/546/368/534; the site refresh KT-610 follows
qualification. DCO/history reconciliation and publication are separate work.
Integration/runtime receipts and live task status belong to the shared plan.
