# Kronn 0.11.0 — Pilot cycle record (KT-377)

This document records the observed identifiers and validation evidence for the
non-critical delegated-task pilot that exercises the 0.11.0 task-orchestration
path. It is the pilot evidence referenced by `docs/releases/0.11.0-checklist.md`
(§ Pilot). No code or schema change is part of this task.

## Scope

- Task reference: `KT-377`
- Task title: `Pilote 0.11.0 — cycle réel d’une tâche déléguée non critique`
- Parent task: `KT-326` — `Livraison 0.11.0 — Documentation, tour guidé, migration et validation pilote`
- Project: `a0f959a8-6a6b-49ac-b4c5-61c0f3c9b385`
- Priority: `normal`
- Final status: `Done`

## Execution identifiers (observed)

| Field | Value |
|-------|-------|
| Task execution id | `c643f291-1def-4fad-8465-c71e5872b4aa` |
| Orchestration run id | `25289d63-12f8-41d8-b48a-6234e5bc8173` |
| Orchestration run kind | `single_task` |
| Idempotency key | `kt377-release-pilot-v2` |
| Dispatch job id | `e092c5e1-186e-4d4f-89e5-e2769f200bbb` |
| Parent discussion id | `309849aa-74aa-4d16-bc59-54be6ca6cfd0` |
| Child (sub) discussion id | `465c5c4e-fa48-42e6-834c-99549c01c07f` |
| Workspace id | `f6512f07-18d9-4ef4-92c3-8c0c94eddfb6` |
| Child branch | `kronn/task/kt-377-c643f291` |
| Pinned base SHA | `e8f916f76e6eb925be9182d7e270f659175a0d65` |
| Target branch | `feat/0.11.0-task-orchestration` |
| Worker agent type | `Ollama` |
| Max review rounds | `3` |
| Review rounds used | `0` (the first delivery was approved) |
| Delivered worker HEAD | `88184252` |
| Candidate target SHA | `cc3dd82f27418275450edf29fd07952ce3611dd6` |
| Candidate merge SHA | `7be448ad907ffbab73c9e1b7e860b16db7ba2cfb` |
| Integrated SHA | `7be448ad907ffbab73c9e1b7e860b16db7ba2cfb` |
| Backup ref | `refs/kronn-backup/ea0b79af-6556-4993-9565-89773eddd309-c643f291` |
| Execution status | `Done` |
| Created at | `2026-08-23T00:24:49.008580Z` |
| Finished at | `2026-08-23T01:18:14.162381Z` |

## Recovery / reassignment trail

The pilot was interrupted twice before this record was written; both events are
recorded here so the audit trail stays coherent (checklist § Pilot, last item).

1. **First interruption** — a non-progressing tool loop was stopped by Kronn
   (`task_get` retried twice, one error). The worker had gathered context but
   had not yet produced the synthesis or the document.
2. **Backend hot reload** — the backend restarted before the agent response was
   persisted. Recovery action: `resume_worker`, reason: *"Backend hot reload
   interrupted the resumed worker; continue the same pilot in the preserved
   child discussion and worktree."*
3. **Bounded reassignment — generation 1** — the execution was resumed in the
   same child discussion and the same worktree. Existing history, manifests,
   findings and SHAs remain authoritative; only the unfinished step (writing the
   document and submitting the typed delivery) was re-run.

Recovery record (observed):

| Field | Value |
|-------|-------|
| Task execution id | `c643f291-1def-4fad-8465-c71e5872b4aa` |
| Assignment generation | `1` |
| Recovery action | `resume_worker` |
| Recovery reason | Backend hot reload interrupted the resumed worker; continue the same pilot in the preserved child discussion and worktree. |
| Watchdog redispatches | `0` |
| Pending | `false` |

## Validation evidence

The pilot's purpose is to exercise the orchestration path, not to run the full
release gate suite. The following is what was actually observed during the
execution:

| Check | Status | Evidence |
|-------|--------|----------|
| Isolated worktree created and attached | pass | `task_exec_status` lineage: `workspace_id=f6512f07-18d9-4ef4-92c3-8c0c94eddfb6`, `child_branch=kronn/task/kt-377-c643f291`, `state=attached` |
| Pinned base respected | pass | `base_sha=e8f916f76e6eb925be9182d7e270f659175a0d65` matches the pinned base in the task brief |
| No manual database edit | pass | No `task_update`/DB write was performed by the worker; the only mutation is the document file in the isolated worktree |
| Recovery after interruption | pass | `recovery.recovery_action=resume_worker`, `assignment_generation=1`, `pending=false` |
| Typed delivery reaches parent review | pass | Delivery `delivery:c643f291-1def-4fad-8465-c71e5872b4aa:0` pinned worker HEAD `88184252`; review `review:c643f291-1def-4fad-8465-c71e5872b4aa:0` recorded `approve` |
| Guarded integration | pass | Target CAS anchor `cc3dd82f…`; candidate and integrated SHA `7be448ad…`; backup ref persisted before apply; execution reached `Done` |

The recovery fixes exercised during this pilot were validated with
`cargo test --lib orchestration` (221 tests) and
`cargo clippy --all-targets --all-features -- -D warnings` (no error). The
final release candidate gates remain recorded separately in the release
checklist.

No project test suite was executed as part of this pilot task: the task scope is
documentation-only (a single new file under `docs/releases/`), and the checklist
§ Candidate gates are run on the final candidate commit by the release owner, not
by this pilot. This is recorded as a limitation, not a skipped gate.

## Definition of Done status

| DoD id | Sentence | Met | Evidence |
|--------|----------|-----|----------|
| `eb864414-879e-4449-a351-2cb17dca28a1` | Le worker crée uniquement `docs/releases/0.11.0-pilot.md` dans sa sous-discussion et son worktree isolé, avec les identifiants d’exécution et les validations observées. | yes | This file is the only file created in the isolated worktree `f6512f07-18d9-4ef4-92c3-8c0c94eddfb6` on branch `kronn/task/kt-377-c643f291`; it records the execution identifiers and the validation evidence above. |
| `467dbbdc-b4be-4d32-9854-5c53cfdbb59d` | La livraison typée atteint la revue du parent sans modification manuelle de la base. | yes | The delivery is submitted through `task_exec_deliver` (typed `DeliveryManifest v1`); no manual database edit was performed. |
| `b791e807-9342-4ebe-8ac7-9871f91634a0` | Le document et le DeliveryManifest consignent les preuves disponibles avant décision (task, execution, discussions, base/head et validation), permettant au parent de déclencher l’intégration protégée. | yes | This document and the accompanying `DeliveryManifest v1` record task reference, execution id, parent/child discussion ids, pinned base SHA, recovery trail and validation evidence. |

## Notes for the parent reviewer

- The only file touched by this execution is `docs/releases/0.11.0-pilot.md`
  (added). No code, schema, migration or configuration file is modified.
- The execution was interrupted twice (tool-loop stop, then backend hot reload)
  and resumed once (generation 1). The recovery trail is recorded above so the
  audit remains coherent.
- The parent approved the first typed delivery and Kronn completed the guarded
  integration into `feat/0.11.0-task-orchestration`. No manual database edit
  was used.
