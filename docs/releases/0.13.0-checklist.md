# Kronn 0.13.0 qualification checklist

This is an in-progress evidence ledger, not a release approval. The shared plan
remains authoritative for task status. Never carry an earlier executable tree's
results forward as proof of a later candidate without rerunning affected gates.

## Identity and boundaries

- This revision records the principal integration of KT-628 on candidate
  `d1e9392944173463966d738327523a5b6b39c348`, frontend tree
  `355a5c5e1a665f3bd80ed825bcdb8b233fbf7a66`; backend `ab1476a0` is unchanged.
  Historical comparison identity is explicitly attested, recorded or unknown,
  with no live configuration substituted for history. The failed automatic
  gate and human-approved takeover are recorded below; KT-630 remains separate.
  See [comparison identity and recovery](../gotchas/compare-model-identity.md).
- The preceding revision records the partial KT-629 join-instruction correction on
  backend tree `ab1476a042ea819b0140791a86467937570c0c6e`, based on `c84dd87d`.
  Frontend tree `078afdc213155e07607026fcc448f9265f527f62` is unchanged.
  Plan reading precedes substantive replies; active work alternates with bounded
  listening, while idle listening remains unbounded. Runtime wait/routing and
  authority functions are untouched. Python instruction alignment awaits
  coordination with KT-619; KT-629 remains open and this is not live-host proof.
  See [joined CLI work loop](../gotchas/joined-cli-work-loop.md).
- The preceding revision brings workflow compensation Agent steps onto the shared
  searchable agent/tier picker: frontend tree
  `078afdc213155e07607026fcc448f9265f527f62`, based on `e1aa3fe0`;
  backend `f607f64776556e2b151e0561907887c1e1889a9c` remains unchanged.
  Explicit target changes clear only model-specific overrides; unchanged
  compensation settings and the main pipeline are preserved. This checkpoint
  does not qualify remaining orchestration launch/reassignment selectors,
  KT-628's separately reviewed comparison changes, browser layout or providers.
  See [compensation selectors](../gotchas/agent-tier-catalogue-settings.md#workflow-compensation-targets).
- The preceding revision shares composer mention tier controls and catalogue resolution
  on frontend tree `541d794f8ff74c925c2b5b1f4316dc8e013bee63`, based on
  `def6da18`; backend `f607f64776556e2b151e0561907887c1e1889a9c` is unchanged.
  Exact HTTP namespace/provenance, unavailable-tier refusal, untouched
  preferences and stable joined-CLI routing have component regressions.
  KT-531 remains open for the full cross-surface selector audit and KT-628's
  separate comparison delivery/review. No browser or provider proof is implied.
  See [composer catalogue contract](../gotchas/agent-tier-catalogue-settings.md#composer-mention-catalogue-contract).
- The preceding revision added the shared searchable agent picker on frontend tree
  `3c05e3eadef5d8df94542c5dd496e92594d18272`, based on `205ac0db`.
  The exact backend tree remains `f607f64776556e2b151e0561907887c1e1889a9c`.
  Search, disabled-choice navigation, provenance descriptions and focus are
  covered by component regressions; composer mentions and comparison display
  paths still keep KT-531 open. See [search and keyboard behavior](../gotchas/agent-tier-catalogue-settings.md#search-and-keyboard-interaction).
- The KT-626 checkpoint uses backend tree
  `f607f64776556e2b151e0561907887c1e1889a9c`, based on `51ba95ca`.
  Workspace tools use the caller's active CLI identity; the durable room-binding
  key remains separate, with no foreign override or implicit reassignment.
  The Python test harness isolates binding writes and unexpected direct HTTP,
  and mocks telemetry outside its dedicated tests. See the recorded checkpoint
  and [workspace identity contract](../operations/mcp-servers/kronn-internal.md#joined-cli-worktrees).
- The principal KT-627 HTTP/Ollama qualification used backend
  tree `f1f41f74d37d489163e0153bf961e8ff3bc9552d`, following the KT-531
  discussion-target checkpoint `076c6c28` and QP/workflow checkpoint `2a7a5645`.
  Frontend tree `081da212a41066fd48c597783dbbb61cea9cb1cf` is unchanged.
  No runtime model is revived from an embedded fallback; unavailable durable
  tier assignments remain visible to preflight instead of silently yielding
  another Default. KT-531 remains open for other custom selectors.
- The qualified KT-621/625 changes follow the integrated
  KT-624 checkpoint `dd697c3e`. Its backend tree is
  `b7f1d4d8b8f8d866d4d41e93c95186a26ec583c7`; the shared plan records integration
  into `feat/0.13.0` and the principal's `codex/kt605-final-review` branch.
  KT-622's frontend correction remains `0b940cf9`; the intermediate
  `636b5263` checkpoint records the issue-206 audit only.
- KT-613 through KT-618 and KT-620 are integrated. This is **not a release approval**:
  cross-surface qualification in KT-543/544/546 remains open. KT-619 implements
  the still-missing structured important-message contract. KT-620 fixes the
  durable CLI return-identity mismatch in `3686c7e`; its full backend replay and
  static checks are recorded separately below. Earlier coverage and browser
  results remain tied to `8ecc3965`, not to this later backend. Completed checks
  apply only to their stated tree; a later product change must rerun affected gates.
- Release notes preparation was approved; version bump, push, tag and publication
  were not performed by this qualification work. `make check-version` still
  checks the synchronized 0.12.0 manifests.
- KT-610's complete site/carousel screenshot refresh is explicitly deferred
  until product qualification is finished. Existing gallery tests are not that
  refresh, and no generated image substitutes for an actual UI capture.

Current blocking work, September 10: KT-531 must finish the remaining common
selector/display paths, and KT-619 must finish verified
Human/Orchestrator publication; the combined KT-621/625 backend qualification
has passed. Human approved KT-619's dedicated admin-bootstrap/Human-credential
contract at 08:11:06 UTC (`kt619-human-credential-bootstrap`); implementation
and review remain required, with no anonymous enrollment or recovery.
KT-627 isolates the HTTP/Ollama fallback portion of KT-531. Its native worker
was interrupted at 17:04:36 UTC without a delivery manifest; its three modified
files remain preserved in the child worktree. The human answered the durable
`kt627-interrupted-worker-recovery` question with `principal-takeover` at
17:55:51 UTC. The execution was cancelled with preservation at 17:56:48 UTC;
its terminal state and unchanged file hashes were reread. The principal recovered
reviewed portions of the diff and supplied independent production regressions,
without modifying the preserved worker tree or launching another worker.
Cancellation did not report confirmed OS termination. No worker validation was
accepted implicitly, no timeout was increased, and the final principal
qualification is recorded below separately from the failed intermediate run.
KT-624 is integrated and prevents native dispatch into a CLI-owned child room,
with its qualified backend tree recorded below. The maintainer explicitly requires
all known defects fixed before 0.13.0, with no implicit deferral or DoD waiver.
KT-621's one authorized Claude attempt stopped at the 120-minute limit on
September 8 at 18:22:34 UTC without a delivery manifest. The human decision at
20:06:03 UTC authorized principal takeover after KT-624. The execution was
cancelled with artifact preservation on September 9 at 06:00:45 UTC; the
principal recovered the worker commits and uncommitted regression into the
review worktree, without editing the preserved worker tree or starting another
worker. Earlier table rows describe historical attempts, not the current state.

## Recorded checkpoints — not final-candidate gates

| Check | Result and exact perimeter |
| --- | --- |
| Principal comparison integration (KT-628), September 10 | Candidate `d1e9392944173463966d738327523a5b6b39c348`, frontend `355a5c5e1a665f3bd80ed825bcdb8b233fbf7a66`, backend `ab1476a0` unchanged. Worker delivery `1667efc1` reviewed independently: **37 focused tests PASS**. Automatic gate from the wrong working directory failed (24 tests + 1 suite); its result remains durable. Human approved principal takeover at 05:51:28Z, cancellation with preservation at 05:53:50Z; termination was not confirmed. Same candidate replayed from frontend: **313 files / 4,063 tests PASS**, 54.17 seconds, unfiltered run 87630. Native/legacy TypeScript, Oxlint, ESLint (0 errors/63 unchanged warnings), i18n (4,481 × 4; 706 static unused warnings), Vite (8.08 seconds), diff and exact 11-file inventory passed. Both local branches advanced by fast-forward; worker worktrees/manifests/commits and three user files are preserved. No database rewrite, replacement worker, push/tag, browser, coverage or provider claim. Execution remains Cancelled: task completion rests on these principal proofs, not fabricated automatic success. KT-630 and the remaining KT-531 surfaces are separate. |
| Join instruction checkpoint (KT-629), September 10 | Backend tree `ab1476a042ea819b0140791a86467937570c0c6e`, based on `c84dd87d`; frontend `078afdc2` unchanged. Three emitted-contract regressions first failed; all **6 join tests PASS** afterward. Existing Python wait tests: **22 PASS**, 2.669 seconds, without wait/routing changes. Unfiltered offline backend run 35315 finished with **exit 0**; the captured final output was truncated, so no aggregate count is asserted. Strict all-target Clippy passed in 15.08 seconds; existing ts-rs warnings remain. Formatting and diff checks pass. These tests qualify source instructions, not host compliance or deployment. Python catalogue/manual/bootstrap alignment is still pending coordination with the KT-619 owner; no authority changes, host settings, provider request, restart, browser or new coverage qualification. KT-629 stays open. |
| Workflow compensation selectors (KT-531), September 10 | Frontend tree `078afdc213155e07607026fcc448f9265f527f62`, based on `e1aa3fe0`; backend `f607f647` unchanged. Five picker regressions failed before correction (3 other rollback checks passed); the shared selector now covers explicit family/tier/named-connection changes, unchanged settings, exact namespace search and unavailable tiers without mutating ordinary steps. Focused: **2 files/108 tests PASS**, 2.51 seconds. Full unfiltered run 28555: **312 files/4,047 tests PASS, 0 failed**, 54.70 seconds. Native/legacy TypeScript, Oxlint, ESLint (0 errors/63 existing warnings), i18n (4,480 × 4; 706 static unused warnings, including the replaced old agent-label key) and Vite (7.14 seconds) passed. Existing act/diagnostic/guarded-unmocked-fetch warnings remain. No coverage, browser, backend, compensation execution or provider proof; KT-531 remains open and KT-628/KT-619 remain separately owned. |
| Composer mention catalogue (KT-531), September 9 | Frontend tree `541d794f8ff74c925c2b5b1f4316dc8e013bee63`, based on `def6da18`; backend `f607f647` unchanged. Six valid regressions failed before integration of the shared resolver/snapshot reader and mention tier component. The first test fixture used an ineligible connection preset; it was corrected and all six failures reproduced against the unchanged palettes. Later tests preserve explicit tier/connection payloads, untouched preferences, unavailable-tier skipping and exact joined-CLI ordinals. A final independent RED reproduced a disabled tier's pointer event selecting its row's available default; the row now refuses that event without substitution. Final focused replay: **4 files/77 passed**, 1.53 seconds. Earlier full replay: 4,040 passed, 56.98 seconds; final unfiltered replay 15231: **312 files/4,042 passed, 0 failed**, 57.50 seconds. Native and legacy TypeScript, Oxlint, full ESLint (0 errors/63 existing warnings), i18n (4,480 keys × 4; 705 static unused-key warnings), and Vite (5.83 seconds) passed. A targeted zero-warning ESLint invocation failed only on existing warnings, independently compared to the unchanged form; no rule or threshold was loosened. Existing act/diagnostic test warnings remain. No backend, new coverage, browser, provider or Important-authority qualification. KT-531 remains open, and the KT-628 comparison worker is reviewed separately. |
| Shared agent-picker search (KT-531), September 9 | Frontend tree `3c05e3eadef5d8df94542c5dd496e92594d18272`, based on `205ac0db`; backend `f607f647` unchanged. Six search regressions failed before correction; a further Escape-propagation regression failed before its fix. Search matches agent/connection identities and configured or assigned model IDs/aliases without selecting or refetching; unavailable choices and explicit unknown IDs remain visible, with accessible provenance descriptions. Keyboard navigation skips disabled choices, Escape stays within the picker, and completed persistence returns focus. Expanded replay first passed 129 cases/failed one old header portal-parent assertion; the assertion now verifies the dialog portal and search outside its menu. Final expanded replay: **130 passed/6 files**, 2.22 seconds. First full frontend run 73067: 4,031 passed/1 failed in 50.89 seconds, on the equivalent pipeline portal assertion. After that fixture correction, 22 pipeline tests passed (1.37 seconds). Final unfiltered run 31582: **311 files/4,032 passed, 0 failed**, 50.04 seconds. Native and legacy TypeScript, Oxlint, full ESLint with the unchanged 63-warning ceiling (0 errors/63 warnings), i18n (4,480 keys × 4), Vite (3.22 seconds), diff and bounded references (11 files/212 citations/14 local links; 14 external URLs excluded) passed. Existing test-harness warnings remain; no browser, new coverage or provider qualification. KT-531 remains open for composer mentions and comparison display paths. |
| Active workspace-tool identity (KT-626), September 9 | Backend tree `f607f64776556e2b151e0561907887c1e1889a9c`, based on `51ba95ca`; frontend `081da212` unchanged. Initial isolated workspace regressions: **8 failed/5 passed**; corrected helper: 13 passed. A separate catalogue-description regression failed before correction; final focused workspace/harness/telemetry replay: 26 passed (0.125 seconds), including 14 workspace cases. Nominal intermediate Python passes were excluded after logs exposed ambient metadata HTTP and telemetry collection; the strengthened HTTP guard reproduced the missing fixture as a teardown error even though production swallowed the transport exception. Scoped mock cleanup and explicit fixtures corrected it. Final full Python helper run 68051: **792 passed**, 0 failures, 9.522 seconds, with no unexpected direct `urlopen` call in the bridge test module. Final unfiltered backend run 6280: **6,857 passed, 0 failed, 6 existing ignored**, 21 suite results; library 6,061 passed/3 ignored in 383.27 seconds, API 595 passed in 156.20 seconds. Unchanged frontend replay: **311 files/4,024 passed**, 86.22 seconds. Strict all-target Clippy passed in 18.99 seconds; formatting, raw-command/TOML/with_conn guards, 7 lint regressions, 664 generated exports without drift, context/MCP gates and their 11+11 regressions, and the 10-case task matrix passed. Context remains 18,842/18,968 bytes; the 110-tool MCP surface and its tightened ceiling are 86,900 bytes. Bounded references: 11 files/205 citations/13 local links, 14 external URLs excluded, no path/line errors. No new coverage, browser, real provider, release publication or Important-authority qualification; no real workspace declaration, lease acquisition or user configuration change was used as a test. |
| Principal HTTP/Ollama model resolution (KT-627), September 9 | Backend tree `f1f41f74d37d489163e0153bf961e8ff3bc9552d`, based on `076c6c28`. Production-library regressions first produced 3 failures/1 pass (0.31 seconds): missing model, actual SQLite deletion/cache refresh, and whitespace values still dispatched. Seven unit resolver regressions rejecting embedded defaults and the obsolete Ollama command-builder case were also independently RED. First corrected runner replay: 251 passed/1 failed/1 existing ignored; the plugin-command assertion incorrectly included the obsolete HTTP-only branch. Its real-CLI scope plus the separate non-inference sentinel regression passed 252 cases/1 existing ignored (5.76 seconds). Intermediate full run 34648 exited 101: 6,056 passed/5 failed/3 ignored in 393.87 seconds, no integration suites. Two fixtures expected unconfigured model names; three preflight cases exposed reliance on the unit-only resolver. A further production regression proved an unavailable assigned tier could be hidden by an available HTTP Default (0 passed/1 failed, 0.06 seconds). Durable identity lookup fixes that case, preserves explicit choices/exact connections and fails closed on assignment-read errors; no-model targets do not acquire new discovery behavior. Final focused checks: 7 production integrations (0.46 seconds), 10 catalogue tests (2.09 seconds), and the worker-catalogue/snapshot cases all pass. Final unfiltered backend run 54450: **6,857 passed, 0 failed, 6 existing ignored**, 21 suite results; library 6,061 passed/3 ignored in 375.59 seconds, API 595 passed in 157.31 seconds, new HTTP suite 7 passed in 0.46 seconds. Strict all-target Clippy passed in 25.73 seconds; formatting, raw-command/TOML/with_conn guards, unchanged context/MCP budgets and regressions, task matrix, 664 generated exports without drift, and bounded references (10 files/127 citations/12 local links; 14 external URLs excluded) passed. Frontend `081da212` unchanged. No new coverage, browser, real provider, credentials, ACP/resume/Copilot behavior or Important-authority qualification. See [resolution and preflight boundaries](../operations/ollama-local-models.md#model-resolution-precedence). |
| Discussion target persistence checkpoint (KT-531), September 9 | Backend tree `793ca114fd39973540e32723a4ce1188873d51bf`: the initial five-case diagnostic produced 4 failures/1 pass; the expanded unchanged-backend suite produced 6 failures/1 pass. After correction all 7 isolated Router/SQLite cases passed, including explicit null versus absent, refusal before mutation, unchanged-target preservation and historical answer preservation. Final unfiltered backend replay (session 70104): **6,848 passed, 0 failed, 6 existing ignored**, 20 suite results; library 6,059 passed/3 ignored in 374.44 seconds, API 595 passed in 153.56 seconds, new integration suite 7 passed in 1.49 seconds. Strict all-target Clippy passed in 34.48 seconds; fmt, raw-command/TOML/with_conn guards, 7 lint regressions, context/MCP budgets and their 11+11 regressions, and the 10-case task matrix passed. Bootstrap stays 18,842/18,968 bytes; 110 MCP tools stay 86,901/86,901 bytes. All 664 type exports assemble without drift; bounded documentation checks resolve 9 files/102 citations/11 local links, with 14 external URLs excluded. Frontend `081da212` is unchanged. Per-setter SQL assignment/model-clear is atomic, not a claim that the entire multi-field PATCH is transactional. No provider, browser, new coverage, HTTP/Ollama fallback or Important-authority qualification is claimed. |
| Explicit QP/workflow target checkpoint (KT-531), September 9 | Frontend tree `081da212a41066fd48c597783dbbb61cea9cb1cf`: two regressions first failed on unchanged production code because switching retained the old connection/reasoning; the unchanged-target preservation case passed. Expanded focused run: 4 files / 146 passed, 0 failed, 9.06 seconds, covering named connections in the wizard and pipeline and actual QP/workflow payloads. An intermediate neighbouring callback test required its new explicit connection argument. First full replay: 4,023 passed/1 failed because an old QP-launch assertion still expected the previous provider's reasoning; the assertion and returned fixture now check cleared reasoning/connection and preserved token limit/original object. Its focused replay passed 13 cases. Final unfiltered replay: **311 files / 4,024 passed, 0 failed**, 51.75 seconds. Native/legacy TypeScript, Oxlint, full ESLint (0 errors/63 unchanged warnings), i18n (4,478 keys × 4), Vite (5.67 seconds), diff and bounded documentation references (9 files/94 citations/11 local links) passed. Backend `b7f1d4d8` unchanged; no browser, coverage, provider execution or discussion-PATCH correction is claimed. |
| Shared model/reasoning form checkpoint (KT-531), September 9 | Frontend tree `958bd053ea6e32a826fb4a97dec028fc7fd1b955`: five QP regressions failed on unchanged production code, including reasoning/max-token loss on save. After fixing the new wizard test's advanced-panel locator, a separate reverse-RED replacing only the new widget with the old model input failed on the missing combobox; the widget was restored before qualification. Expanded focused replay: 5 files / 117 passed, 0 failed, 2.02 seconds. Final unfiltered frontend replay: **310 files / 4,017 passed, 0 failed**, 57.38 seconds. Native/legacy TypeScript, Oxlint, full ESLint with unchanged 63-warning ceiling (0 errors/63 warnings), i18n (4,478 keys × 4), Vite (9.84 seconds) and bounded documentation references (9 files/82 citations/11 local links) passed. Test-harness diagnostic/act warnings remain; no browser, coverage or provider test is claimed. Backend `b7f1d4d8` is unchanged. Catalogue reads never call Ollama discovery; unknown explicit IDs and reasoning remain visible, known unavailable models stay disabled, and QP saves preserve unrelated maximum-token settings. KT-531 is not closed. |
| Shared selector identity checkpoint (KT-531), September 9 | Frontend tree `dd91b2e3a7a38cb7d10f8e79a49b5ddc5add0cf4`: 8 regression failures on unchanged production code (7 picker cases plus the embedded display fallback) reproduced explicit-ID precedence, unavailable selection, stale/failed snapshot provenance and runtime isolation. Expanded targeted run: 110 passed; subsequent header/QP/picker replay: 80 passed. The first full run had 4,003 passed/2 failed: the header fixture relied on an unconfigured embedded Sonnet, and the QP card failed to show its actual saved override. The fixture now explicitly configures its asserted model; production header/QP displays now receive the saved item override. Final unfiltered frontend replay: **308 files / 4,007 passed, 0 failed**, 52.94 seconds. Native/legacy TypeScript, Oxlint, full ESLint with the unchanged 63-warning ceiling (0 errors/63 warnings), i18n (4,478 keys × 4), Vite (4,517 modules, 1.09 seconds) and bounded documentation references (9 files/72 citations/10 local links) passed. Backend `b7f1d4d8` is unchanged. No browser, coverage or provider qualification is implied; KT-531 stays open for other custom selectors and the HTTP runner fallback. |
| Agent-card catalogue checkpoint (KT-531), September 9 | Frontend tree `64304d5093f716964ed67f8aba1abc0563ad234b`: seven component regressions first failed on unchanged production code; all passed after correction. Expanded coverage: 13 catalogue/preservation cases plus 3 shared-picker accessibility cases passed. The neighbouring settings/catalogue suite passed 81 cases. Unfiltered full frontend replay: **306 files / 3,990 passed, 0 failed**, 54.30 seconds (`--maxWorkers=4`). Native and legacy TypeScript, Oxlint, targeted and full ESLint (0 errors/63 existing warnings), i18n (4,478 keys × 4; 705 static unused-key warnings) and Vite build passed. Test logs still contain expected diagnostic and act() warnings; this is not a warning-free test-harness claim. Backend tree remains `b7f1d4d8b8f8d866d4d41e93c95186a26ec583c7`. No new coverage/browser/provider run. The separate embedded display fallbacks keep KT-531 open. See [settings preservation](../gotchas/agent-tier-catalogue-settings.md). |
| CLI-owned room dispatch (KT-624), September 9 | Backend tree `d16372f1d8b067a5c9bffc06943e1062e13c297d`, built only in the principal worktree's dedicated target. Two real provisioning/API/claim regressions failed before correction; the expanded five-case suite exposed and then verified explicit native reassignment, claim-to-start ownership change and database reopen with an exhausted legacy job. Final targeted run: 5 passed, 0 failed, 0 ignored, 1.57 seconds. Unfiltered backend replay: **6,820 passed, 0 failed, 6 existing ignored**, 19 suite results; library 6,038 in 349.49 seconds, API integration 595 in 162.13 seconds. Strict all-target Clippy passed in 36.72 seconds; fmt, diff, raw-command/TOML/with_conn guards and all 664 generated exports passed without drift. The first sandbox attempt could not run loopback fixtures and was interrupted; only the explicitly authorized replay supplies the full-suite result. No provider prompt, live-instance test, identity migration, worker619/621 source edit, new browser run or coverage claim. |
| Frontend unit/coverage, September 7 | 304 files / 3,970 tests passed, no failures or skips, in 39.01 seconds. Frontend tree `d3d36ceaf79ba656aa1180070a6cf809069ae791` (includes KT-614), unchanged through `8ecc3965`. Statements 75.74%, branches 72.12%, functions 71.24%, lines 78.94%. Report: `/private/tmp/kt546-679c9fda-frontend-coverage/coverage-summary.json`. |
| Frontend static/build, September 7 | Same frontend tree: native and legacy TypeScript, oxlint and production Vite build passed; CI ESLint scope `src/ --max-warnings 63` passed with 0 errors/63 warnings. i18n: 4,473 keys per locale, 0 errors/705 unused-key warnings. Vite completed in 3.90 seconds with existing dependency/chunk warnings. |
| Full browser suite, September 7 | Exact committed backend/frontend/site checkpoint `8ecc3965`, rebuilt in isolated offline-cache Docker images: 110 passed, 18 skipped, 0 failures, 0 flaky cases and 0 retries in 325.824 seconds (start 17:01:37 UTC). The targeted QA CRUD replay also passed (1 test, 1.589 seconds). Retained report: `/private/tmp/kt546-e2e-8ecc3965.AbtIpz/browser-results-r2/playwright-results.json`; adjacent `qa-results.json` records the targeted run. |
| Mobile tour (KT-612) | 36 targeted unit tests and 4 Chromium checks passed; 26 steps at each of 1280×800, 1280×620 and 720×900, plus durable resume. Final worker revision `bb2eb29aef8cfeb9909d8060bd2b8d2256dd8d65` was reviewed and integrated. |
| OpenCode prompt | The missing native MCP guidance was reproduced before correction; 28 prompt and 3 ACP-registry tests passed after `36d8ad4`. |
| OpenCode database identity | Native launch failed before dispatch because the database reader omitted OpenCode. Regression reproduced the failure; all 75 database-orchestration tests passed after `64ecf135`. |
| Python/shell, September 7 | 783 bridge/helper tests, 14 documentation-sidecar tests and 275 Bats tests passed on the relevant tree shared by `349b63ab` and `8ecc3965` (their difference is site HTML only). The sidecar's loopback fixtures used explicit execution permission. Bats ran from the main checkout's existing submodule; the review checkout had no initialized Bats submodule. Backend CI SLO regression also passed. |
| Policy budgets, September 7 | Context budget, MCP budget and agent-task matrix passed at `8ecc3965`: bootstrap 18,842/18,968 bytes; 110 MCP tools, 86,901 bytes with an exact 86,901-byte ceiling; 10/10 agent-task matrix cases. Budget regressions and all 7 `with_conn` lint regressions passed; the live `with_conn` lint passed. |
| Full backend, September 7 | Unfiltered `cargo test` passed at `8ecc3965`: 6,798 passed, 0 failed, 6 existing ignored tests across 19 suite results, including 6,016 library tests and 595 API integration tests. Library execution took 349.29 seconds; API integration execution took 150.71 seconds. Loopback fixtures used explicit execution permission. This includes type exports and is not a `--lib`-only run. |
| Backend static/types, September 7 | Strict `cargo clippy --all-targets -- -D warnings` passed in 51.04 seconds; formatting, the raw-command guard, TOML parse guard and `with_conn` lint passed. Assembling all 664 exported types produced no drift in `frontend/src/types/generated.ts`. |
| Backend coverage, September 7 | `cargo llvm-cov --lib --tests --summary-only` exited 0 at `8ecc3965`, with all three `--fail-under-* 83` gates enabled: regions 84.29%, functions 83.90%, lines 83.81%. The separate key-management script reused that instrumented report and passed all seven module floors (crypto, config, keyvault, keystore, recovery, MCP persistence and migrations). No floor was lowered and no ignored test was counted as a pass. |
| Desktop, September 7 | `cargo check --manifest-path desktop/src-tauri/Cargo.toml --locked` passed in 56.63 seconds on macOS at the `8ecc3965` desktop tree. This is a native compile check, not Linux/Windows qualification or a packaged desktop release. |
| Duplication and dependency audits, September 7 | jscpd v4: 684 files, 153,462 lines, 308 clones, 3,998 duplicated lines, 2.61% under the unchanged global 3% ceiling (3.893 seconds). Backend `cargo audit` exited 0 after refreshing RustSec: 411 dependencies, one previously documented unmaintained `ttf-parser 0.25.1` warning (`RUSTSEC-2026-0192`), no new ignore. Frontend `pnpm audit --audit-level=moderate`: no known vulnerabilities, exit 0. No dependency or threshold was changed. |
| CLI recovery (KT-615) | At `2e57be50`: 4 focused restart regressions, 336 orchestration tests, formatting and strict all-target Clippy passed. The exact worker may reside in the pinned child; unrelated identities and quota gates remain refused/preserved. See `docs/testing/cli-worker-restart-recovery.md`. |
| Durable CLI return identity (KT-620), September 7–8 | Commit `3686c7e`: initial cancellation/reassignment regressions failed before correction and passed after it. Nine DB identity tests and eight API lifecycle regressions cover exact-key persistence, transaction rollback, restart, replacement and stale-offer refusal. Final unfiltered backend replay: 6,815 passed, 0 failed, 6 existing ignored; library 6,033 in 372.93 seconds, API integrations 595 in 153.61 seconds. Strict all-target Clippy passed in 37.29 seconds; formatting, diff, raw-command/TOML/with_conn guards and 664 generated exports passed without drift. Migration 171 is additive, with no inferred historical binding repair. This does not qualify later KT-619 changes or claim a live return of its pre-migration worker. |
| Target checkout (KT-616) | At `679c9fda`: 6 targeted regressions, 339 orchestration tests and 92 worktree tests passed, plus formatting and strict all-target Clippy. Native integrations of KT-613 and KT-617 subsequently advanced only the pinned target despite unrelated untracked main-checkout files. See `docs/testing/pinned-target-worktree.md`. |
| Principal evidence (KT-613) | Principal independently ran 51 delivery tests, formatting and strict all-target Clippy at delivered HEAD `068927080cb313f623c1359c05fe32be0b567b6e`; all passed. The actual KT-617 attempt-1 report then published 3 separate principal verifications alongside the 4 unchanged worker validations and the worker's no-browser-rerender limitation. |
| Agent-family site copy (KT-617) | Delivered HEAD `36867383c9e6ae9cf7f831fd49f177ff8a32a00f`: 18 browser cases passed (FR/EN/ES at 1280, 980, 769, 768, 390 and 320 px). CLI/HTTP examples include the requested ellipsis; Local remains Ollama. No card or SYSTEM MAP overflow. At 769 px, unrelated inventory overflow is unchanged from the baseline: FR 14 px, EN 19 px, ES 26 px; tracked in KT-610. These are temporary test captures, not the final gallery refresh. |
| Independent recovery review (KT-618) | Final report `ab0a8f343584ee2aa36f6c795230800662580aeb`, approved attempt 2 and natively integrated as `1187a1ba`. Read-only code/runtime review, no worker tests claimed. The accepted report contains three separately attributed principal verifications. Findings preserve the observed binding mismatch without claiming an established cause, and document conservative initial-offer cancellation/re-preflight rather than unsupported reassignment. See `docs/testing/agents-v2-recovery-independent-review.md`. KT-620 follows the later source-identity diagnosis; it does not rewrite that accepted delivery. |
| Bounded documentation references, September 8 | Five documentation files changed between `8ecc3965` and `30826755`: all 22 file citations resolve with valid line bounds and all 3 local Markdown links resolve. The read-only checker is retained at `/private/tmp/kt546-doc-audit.ZxlIpW/check-local-references.mjs`. This is a path/line check, not a semantic citation review or validation of fragment anchors/all repository documentation. |
| Bounded documentation/version follow-up, September 8 | Rechecking the same five files at `e070ff5` resolved 24 file citations and 3 local links with no errors; the two external URLs were excluded from the local checker and checked separately against the official ACP documentation. `make check-version` passed with manifests still synchronized at 0.12.0. The installed OpenCode reported 1.18.27; the official latest release resolved to 1.18.29. No executable or dependency was updated. This is not an all-vendor freshness approval. |
| Bounded release-addition hygiene, September 8 | Read-only diff `0.12.0..e070ff5`: 460 changed paths and 74,005 added lines, no matching generated-artifact paths. The heuristic scanner returned exit 1 for one provider-key-shaped literal in `backend/tests/api_tests.rs:16766`; context review places it in an API round-trip fixture using an in-memory database and a local router request. No credential value was printed. This is a reviewed fixture-context finding, not a clean secret-scan verdict: entropy, deleted/history content and unrelated untracked files were outside the scan. `git diff --check` passed over the same range. Scanner: `/private/tmp/kt546-doc-audit.ZxlIpW/check-release-additions.mjs`. |
| KT-621 intermediate review, September 8 — not integrated | At worker HEAD `d99ebae252e2ee228627d3ab9f45f130cbd7c1b3`, principal `cargo test --lib acp --quiet` completed: 82 passed, 0 failed, 0 ignored, 2.24 seconds of tests after compilation. Formatting passed. Strict all-target Clippy completed with exit 101 on two `needless_option_as_deref` errors in `runner.rs` (3185, 3215). Production discussion/reopen tests are still missing; the principal requested changes for cursor safety, adapted-runtime continuity and the native missing-session path. Attempt 2 is working in the same execution; no result here qualifies the target branch. |
| Native command metadata, September 8 | Installed help for OpenCode ACP, Copilot ACP and Kiro ACP matches the compiled native commands; observed versions are 1.18.27, 1.0.80 and 2.21.1 respectively. Vibe ACP help passed after explicit permission for its local initialization; `uv tool list` reports `mistral-vibe` 2.24.5. Gemini was absent from this shell's PATH; the official ACP page documents the compiled `--acp` flag, but no local execution was tested. The operator matrix links the primary references. No provider prompt, login, installation or update was performed; this is not a multi-provider smoke or an all-versions-current verdict. |
| KT-621 final allowed rework review, September 8 — not integrated | Worker HEAD `0c898a6ef3c0984d9871896c752864cd20753bfd`: principal `cargo test --lib acp --quiet` completed with 82 passed/0 failed/0 ignored (2.18 seconds). The separately selected new `start_agent_with_config_native_route_uses_only_the_explicit_resume_delta` test failed because its expected prompt omitted real system enrichment (0.44 seconds). `resume_delta_tests` completed with 4 passed/2 failed: both new Discussion fixtures lacked `created_at` (0.39 seconds). The input/output checkpoint distinction and production discussion/DB-reopen proof remain incomplete. Review refused; execution `0dcf7ee4-4ec4-484e-8114-a1db01907354` became `Escalated` at 16:12:53 UTC, with four refusals and the unchanged three-reoffer limit. No automatic fourth reoffer, approval or integration. A human card asks whether to authorize one reassigned native Claude Code delivery. |
| Arbitration form refresh diagnosis (KT-622), September 8 | On unchanged production components at `6503ecd1`, three deterministic diagnostic cases passed: a question-store refresh and a same-reference sources rerender preserve the draft, while a fresh equivalent sources array remounts the form and loses input/selection/focus, disabling Answer. The third assertion proves the defect, not a passing product gate. No real answer submission or provider call. See [diagnosis and bounded correction proposal](../gotchas/arbitration-form-refresh.md). Production correction awaits human authorization; no KT-619 worker file was changed by this diagnosis. |
| Arbitration form correction (KT-622), September 8 | Human authorization `fix-with-tests` recorded at 16:20:21 UTC. Four of seven regression cases failed before correction; all seven passed after it, and the focused card/rendering/citation suite passed 123 tests. Final full frontend run: 305 files / 3,977 tests passed, 0 failed, in 208.26 seconds (`--maxWorkers=4`). Native and legacy TypeScript, oxlint, i18n (4,473 keys per locale; existing 705 unused-key warnings) and CI ESLint scope (0 errors/63 unchanged warnings) passed. Vite production build: 4,516 modules, 19.57 seconds, existing dependency/chunk warnings. Renderer types are stable; values remain reactive through context. No backend change, new coverage run or real-browser recording is claimed. |
| KT-621 explicit reassignment, September 8 | Human chose `claude-native-one-pass` at 16:20:30 UTC. Guarded reassignment at 16:22:33 kept execution `0dcf7ee4-4ec4-484e-8114-a1db01907354`, child/worktree and evidence; native worker changed from Codex to Claude Code (default tier), attempt 4, assignment generation 1. The configured three-reoffer budget is unchanged. One new delivery is authorized, with independent review and a fresh human decision on any further insufficiency. This is not an approval or integration. Migration 173 is coordinated for its checkpoint only; 172 remains reserved for KT-619. |
| Issue 206 read-only verification (KT-623), September 8 | At integrated HEAD `0b940cf96b1fc84c6b8049b96ec9f614c11d7f0e`, `cargo test --manifest-path backend/Cargo.toml --locked --offline --lib resume --quiet` completed with 91 passed, 0 failed, 0 ignored, in 11.50 seconds. `CARGO_TARGET_DIR` was the principal worktree's own `target`; the source worktree remained clean. This includes Claude resume-argument and intervening-message delta tests, not a real Claude/LiteLLM conversation or an assertion that no file is reread. An earlier shared-target run executed three KT-621 tests absent from this source tree; its 92-pass/3-fail result is excluded from release evidence. No shared cache was cleaned and no worker source was changed. |

### Principal KT-621/625 qualification, September 9

The final combined backend tree is
`b7f1d4d8b8f8d866d4d41e93c95186a26ec583c7`, based on `dd697c3e`.
All Cargo commands used the principal worktree's own `CARGO_TARGET_DIR`,
`--locked --offline`, and explicit permission for loopback fixtures where needed.

- Unfiltered `cargo test --manifest-path backend/Cargo.toml --locked --offline`
  (session 68046) exited 0: **6,841 passed, 0 failed, 6 existing ignored** across
  19 suite results. Library: 6,059 passed/3 ignored in 381.07 seconds; API:
  595 passed in 159.68 seconds. All remaining integrations and doctests finished.
  This includes type exports, not a library-only or filtered run.
- Strict all-target Clippy with `-D warnings` passed in 15.50 seconds.
  Formatting, raw-command/TOML guards, seven `with_conn` lint regressions and the
  live lint passed. All 664 generated types assemble without tracked drift.
  Existing ts-rs attribute warnings remain; no threshold or ignore was changed.
- KT-621's production decision/start chain now has a persisted two-turn test
  with a full DB close/reopen, exact session scope, its own output marker and
  an interleaved peer of the same provider. RED cases exposed peer loss and own
  response replay before correction. The focused resume suite passed 104 cases
  in 13.21 seconds before the final capability regression; the later ACP suite
  passed 92 cases in 2.07 seconds, including rejection of null/malformed resume
  capabilities. Adapted Claude/Codex fallback was independently RED then GREEN
  for both missing session and unavailable capability. The final full run above
  includes all these regressions. See [the native continuity contract](../gotchas/native-acp-resume-continuity.md).
- KT-625 removes the PID-file startup/read race from the Copilot timeout proof
  and makes timeout handling await child termination/collection. The four-second
  product deadline is unchanged. All 28 focused Copilot tests passed in
  0.15 seconds, including a controlled-clock full-spawn deadline case, immediate
  collected-child check, output capture and spawn failure. No provider runs,
  retry, arbitrary sleep or new ignore. See [the preflight regression](../gotchas/copilot-preflight-timeout.md).

Excluded earlier full-suite attempts are retained as failures, not averaged into
this result. Session 19196 was intentionally interrupted (exit 130) to fix the
new null-capability finding. Session 75315 on backend tree
`8dada437371815cb1d2f8d540473262f0ca62a2a` exited 101: library 6,056 passed,
1 failed, 3 ignored in 374.38 seconds; the Copilot test timed out before its PID
file existed, and integration suites did not run. The independent KT-619 worker
also observed an empty PID file in a filtered library run (5,381 passed,
1 failed, 3 ignored, 674 filtered, 351.67 seconds). Isolated green reruns did not
disprove that race; KT-625 corrected it before the final combined run.

These results do not qualify a live provider, normalize ACP cumulative cost,
refresh coverage/browser evidence or approve later KT-619 changes. Transport
fixtures exercise the real decision/start branches, not a complete live HTTP/SSE
conversation. No user database migration, provider login or release operation
was performed by these tests.

The current full-browser runner uses a private disposable backend on an internal
Docker network, no host mounts, user database or host credentials, and test-only
provider fixtures. Its 18 explicit skips comprise 11 real/billed CLI canaries,
2 local-filesystem/live-instance checks, 3 cases requiring pre-existing media
data, and 2 comparisons requiring at least two installed targets. They do not
validate those providers or fixtures. The first `8ecc3965` run passed 109 cases
and skipped 19 because QA CRUD additionally lacked an API-plugin fixture. A
local custom API fixture with empty credentials/endpoints and host sync disabled
removed that extra skip; no assertions changed. Its loopback port 1 endpoint was
not used for provider calls. The first run remains separately retained at
`/private/tmp/kt546-e2e-8ecc3965.AbtIpz/browser-results/playwright-results.json`.
Earlier `708bf449` browser evidence does not qualify later backend fixes.
Older `/private/tmp` artifact paths may no longer exist after the host reboot;
do not describe them as currently retained without checking.

The six backend ignores are not newly added or silently counted as successes:
three library tests (repository-dependent file inventory, a live LiteLLM loop,
and the documented `fs_guard` symlink-swap probe), two real-agent canaries, and
one runner doctest. In particular, this run does not turn the ignored filesystem
race probe into a security guarantee.

## Remaining final-candidate gates

- [ ] Review and integrate all release-blocking product changes; record final SHA.
- [x] Backend full `cargo test`, including integrations and type exports, at `8ecc3965`.
- [x] Strict all-target Clippy at the same backend tree.
- [x] Repository/key-management coverage floors at `8ecc3965`.
- [x] Backend policy checks, context/MCP budgets and agent-task matrix at `8ecc3965`.
- [x] Python bridge/sidecar and shell suites on the relevant `8ecc3965` tree.
- [x] Frontend unit coverage, both typechecks/linters, i18n and production build on frontend tree `d3d36cea`.
- [x] Full browser suite against backend/frontend/site `8ecc3965`; 18 explicit skip reasons retained. Rerun affected gates after KT-619/620.
- [x] Regenerated bindings checked without drift (664 types).
- [x] KT-620's final backend full suite, strict all-target Clippy and static/type guards at `3686c7e`; final coverage/browser replay remains pending after remaining product changes.
- [x] KT-621/625's unfiltered full backend, strict all-target Clippy and static/type guards on backend tree `b7f1d4d8`; later product changes still require affected gates.
- [x] macOS desktop locked check, duplication and dependency audits at the `8ecc3965` tree, with the existing maintenance warning recorded.
- [ ] Vendor-version freshness, version synchronization and documentation links/citations.
- [ ] Whitespace, secret/artifact hygiene and preserved unrelated user changes.
- [ ] Complete KT-610 after qualification; document all final feature surfaces.
- [ ] Explicit maintainer authorization for any release publication operation.

## Runtime observations still requiring disposition

- [Issue 206](https://github.com/DocRoms/Kronn/issues/206) reports Claude rereading
  project context after a LiteLLM turn. Its proposed explanation, no CLI resume,
  is already addressed by KT-562: commit `0214c132` is a verified ancestor of
  `0b940cf9`. The CLI session identity is scoped to discussion, agent, runtime
  and project; a resumed turn couples that identity with every message after
  the saved marker, including intervening peer messages. The deterministic
  peer test uses generic agent messages, not an actual LiteLLM execution.
  [src: file: backend/src/agents/runner.rs:2147]
  [src: file: backend/src/api/discussions/streaming.rs:1378]
  [src: file: backend/src/agents/runner_test.rs:7526]
  [src: file: backend/src/api/discussions/streaming.rs:5384]
  This does **not** establish the issue's strict no-repeated-Read criterion.
  No real Claude → LiteLLM → Claude tool trace was collected by this audit,
  and the reporter's installed version/configuration is unknown. Missing
  sessions or markers, changed history and a delta without size savings fall
  back to full context. The current session probe uses the default
  `~/.claude/projects` layout, not a relocated `CLAUDE_CONFIG_DIR` store.
  [src: file: backend/src/agents/runner.rs:7788]
  Claude documents saved messages, tool uses and results and same-ID resume;
  that restores session continuity but does not prohibit subsequent verification
  reads. A newly launched process need not mean a new conversation. The exact
  reported scenario still needs validation before issue closure; KT-621 native
  ACP continuity is a separate correction qualified in this revision. No GitHub write or real
  provider call was performed.
  [Claude Code session behavior](https://code.claude.com/docs/en/how-claude-code-works#work-with-sessions)
- KT-614's first real OpenCode worker ran, then ended with a network error and no
  durable manifest. The same execution was explicitly reassigned to Codex; that
  fallback is not proof that the OpenCode provider's network path is stable.
- KT-613's accepted CLI worker moved into its child room, then backend restart
  classification reported it unavailable. KT-615 fixed the exact parent/child
  check; KT-618 independently reviewed that fix and the target-worktree guard.
  Do not infer a bridge-reload grace-period problem from timing alone.
- A later source audit identified different identity inputs on the joined CLI
  outward and return paths: the former receives a durable binding key, the latter
  previously reread the active session row. KT-620 reproduced and fixed that mismatch;
  the stale catalogue entry observed for CLI147 is a separate symptom, not yet
  attributed to this mechanism. Its worker can explicitly reconfirm an existing
  accepted offer after migration, only while still the exact assigned worker.
  KT-619's exact CLI148 reconfirmed its own accepted offer on September 8;
  the principal independently observed `cli_worker_binding_pinned` at
  16:14:22.147900 UTC on the unchanged execution. This proves the live pin,
  not a successful terminal return, which remains unobserved. No historical
  user binding was repaired by the qualification tests.
- Native ACP host loading alone was not production OpenCode continuity: the
  audited `start_agent` passed no resume identity or session store. A
  human-gated proposal for a bounded native delta/resume fix
  was accepted on September 8 at 06:23:56 UTC, creating KT-621 under KT-543
  (`proposal:6ff0a285-c9e2-4b26-a071-8e1d704814d5:0`). Before the qualified
  correction in this revision, four partial deliveries were reviewed and
  the three allowed reoffers were exhausted. The human decision
  `kt621-review-budget-next-owner` subsequently authorized the one native
  Claude Code reassignment recorded above. Attempt 4 subsequently timed out
  without delivery; the authorized principal takeover is now effective. Its
  strengthened real two-turn/reopen regression covers input/output boundaries,
  interleaved same-provider peers and atomic response persistence. Targeted
  tests, Clippy and the final combined full backend pass as recorded above.
  KT-562's Claude CLI-print continuity and KT-577's future long-lived Claude
  process do not substitute for qualification of this native production branch.
- The upstream ACP v1 schema supports optional cumulative session cost with a
  currency. Kronn does not normalize that field yet. The operator matrix now
  describes the implementation limit instead of claiming that ACP cannot carry
  cost. Native session-cost accounting remains outside KT-621's bounded resume
  correction; unknown values are not treated as zero or billed USD per turn.
  [ACP v1 UsageUpdate, checked 2026-09-08](https://agentclientprotocol.com/protocol/v1/schema#usageupdate)
- OpenCode version freshness is recorded, not silently repaired: the host
  reports 1.18.27 while the official latest release checked on September 8 is
  [1.18.29](https://github.com/anomalyco/opencode/releases/tag/v1.18.29).
  Source inspection for native ACP behavior used the installed version's
  pinned source, not an assumption that the host was already up to date.
- KT-613's own accepted report was published before its new renderer was loaded;
  it therefore lacks the principal-evidence section. It is not rewritten after
  the fact. The first observed live report using the new section is
  `delivery-summary:ece6b886-52ca-4173-8ebb-98342e5fee69:1` (KT-617).
- The production PR-review parent run reported success, but four of its 25 child
  runs failed (three Jira-NONE paths and one GitHub review-publication request).
  The GitHub publication outcome is uncertain: no automatic replay is authorized.
