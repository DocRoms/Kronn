# September 13 integrated browser and desktop checkpoint

This is local qualification of `f21f5a1558603e88747b54ad70b4e9508391e5bc`,
not remote CI or release approval. MAIN and the qualification worktree contain
that candidate. Its executable backend/frontend trees and full Rust/frontend
results are recorded in [the important-message qualification](0.13.0-important-messages-qualification.md).
[src: commit: f21f5a1558603e88747b54ad70b4e9508391e5bc]

## Full Chromium replay

The complete suite ran with `CI=1`, `--retries=0`, JSON reporting and no spec
filter. It completed on September 13 at 19:49:50 UTC: **118 passed, zero
unexpected, zero flaky, 19 skipped**, in 329.725 seconds. All nine important
message cases and all three OpenCode settings cases passed.

The retained evidence root is
`/private/tmp/release-013-takeover-proof.XMDShb/`; the report and wrapper receipt
are `release-browser-4EmtAz/playwright.json` and `receipt.json`. The wrapper
exited zero: expected HEAD, clean tracked source, four host MCP configuration
hashes and the MAIN listener PID were unchanged. Its owned process groups were
stopped and its disposable fixture was retained. No provider credentials,
installation or billable-agent opt-in were supplied.

The 19 skips are explicit limits, not successes:

- 11 real/billable-agent canaries;
- two local host-filesystem/live-backend cases;
- three showcases requiring pre-existing generated media;
- two Compare cases requiring at least two installed targets;
- one Quick API CRUD case requiring an API plugin in this empty fixture.

This run covers the available deterministic browser surface, including mocked
provider boundaries; it does not prove live provider inference, every agent's
authentication, Docs export, or the skipped cases. Historical runs that seeded
an API plugin have their own results and are not added to this run's count.

The disposable backend uses the isolated launcher and copied binary SHA-256
`59113941c5aa7331cb06b94dc0bb57a6ab1ac492af9c2699fb585db345d53ebd`.
Its initial setup was completed through the ordinary setup API with its own
empty repository directory, matching the existing CI prerequisite. It never
scanned the user's repositories. The fixture is not an OS/network sandbox.
[src: file: scripts/e2e-sandbox-backend.sh:1]
[src: file: .github/workflows/ci-test.yml:562-581]

Two previous attempts remain retained and do not qualify this checkpoint:
`release-browser-KC3ixF` was interrupted after navigation timeouts because the
fresh fixture still showed the setup wizard, before accessibility scanning;
`release-browser-PRpqwe` passed 118 tests but failed the wrapper's integrity
guard because the host Codex configuration changed during that window. The
writer was not identified, and no user configuration was restored or erased.
The final replay kept the same integrity guard and observed no such change.

## Native desktop check

`cargo check --manifest-path desktop/src-tauri/Cargo.toml --offline --locked`
passed on the same candidate in 138.889 seconds, with its own build target and
data/host roots. Receipt: `f21f5a155860-desktop-integrated-candidate.json` in
the evidence root above. This is a macOS compile check, not packaging, desktop
UI execution, Linux/Windows compilation or fresh remote CI.

## OpenCode acceptance evidence and limits

The combined qualification now includes the earlier distinct corrections:

| Concern | Evidence and exact boundary |
| --- | --- |
| Selection and native identity | [Execution-surface tests](../testing/opencode-execution-surfaces.md) assert native outgoing identities in discussion, QP Compare, workflow, orchestration and judge UI. QA/QE execute API/command contracts and have no agent selector. |
| Models, missing entries and Zen metadata | [Catalogue inventory](../gotchas/model-catalogue-consumer-inventory.md) maps actual consumers and migration; the complete frontend/backend suites include these regressions. No embedded model or tariff is inferred from a provider name. |
| Creation, streaming, errors, usage and cancellation | `run_acp_session_*` tests exercise the real AgentProcess path with a fake ACP transport, including redaction and non-successful cancellation. They passed in the 7,056-test backend run. |
| Resume and restart | [Native ACP continuity](../gotchas/native-acp-resume-continuity.md) uses the production decision/start chain and atomic reply writer across a file-backed database reopen. The two-turn and interleaved-peer regressions passed in that same run, not against a live provider. |
| Costs and statistics | [Cost provenance](../gotchas/stats-cost-usd-not-always-measured.md), aggregate unit tests and real router regressions distinguish unknown, recorded zero, estimates and partial totals. The human's `honest-unknown` decision on September 13 limits this scope; monetary ACP accounting remains excluded. |
| Browser settings | The three OpenCode authentication/permission explanation cases passed in the full replay above. They use controlled API responses and do not install or authenticate a real CLI. |

The separate global spend collector still does not collect OpenCode monetary
usage. Nothing here describes unknown cost as free or a saved amount as an
attested measurement. These boundaries match the accepted targeted correction,
not a waiver inferred from passing tests.

Remote CI/coverage, release-wide upgrade validation, DCO reconciliation, final
site work, version synchronization and publication remain separate gates. The
new proposed important-message authoring UX is not included in this candidate.
The shared discussion plan owns its human decision and live task status.
