# September 13 principal stabilization qualification

This records the combined KT-638/640/641/642 candidate, not release approval.
The exact tested commit is `2d6fc6a46e5ba79d3bab661535040eb5f22448fb`.
The principal preserved the cancelled workers' checkouts and histories under
the recorded human takeover decisions. No new worker, history rewrite, host
configuration repair, cleanup or publication is part of this qualification.
[src: commit: 2d6fc6a46e5ba79d3bab661535040eb5f22448fb]

## Final combined backend

The unfiltered command was:

```sh
rtk proxy cargo test --manifest-path backend/Cargo.toml --offline --locked
```

It ran with two Cargo build jobs, incremental compilation disabled and the
approved line-table development profile. Each invocation owned temporary
`KRONN_DATA_DIR` and `KRONN_HOST_HOME` roots; neither `HOME` nor `CODEX_HOME`
was changed. The replay explicitly received permission to bind local test
servers. The wrapper captured immutable stdout/stderr and before/after Git
HEAD, tracked status, backend diff and the two regression source blobs.

- Exit 0 in 573.228 seconds; no timeout or Git/source drift.
- 6,919 passed, zero failed, six ignored across 25 top-level suites, without
  test filters. Three additional exact-filter child runs each passed their
  isolation check; these are not added to the top-level count.
- Formatting and strict all-target Clippy (`-- -D warnings`) passed on this
  same commit; Clippy took 32.042 seconds.

Local raw evidence is retained in
`/private/tmp/release-013-takeover-proof.XMDShb/`, notably
`2d6fc6a46e5b-full-loopback-authorized.json` and its stdout/stderr logs.
An earlier sandbox run was interrupted after a separately reproduced loopback
`EPERM`; it is retained but is not qualifying evidence or diagnosed product
failure. The same controlled bind succeeded with permission.

## Regression controls and supporting gates

At preceding commit `6b4cc83940779e35a1f176758dcfe88c1eb2c027`, the
reassignment family passed 23 tests. Removing the actual API Provisioning arm
then made the native-dispatch regression fail with Provisioning instead of
Working, not a compilation error. Restoring the exact source made it pass.
The rollback fixture asserts the real missing-child failure boundary and the
entire execution row remains unchanged.
[src: commit: 6b4cc83940779e35a1f176758dcfe88c1eb2c027]

Removing the integration helper's host override made the isolated GREEN child
and its parent fail against disposable inherited-host sentinels. The deliberately
unisolated RED child still passed its positive control. Exact restoration made
the parent and all three children pass. The two-parent concurrent initialization
check also passed. The library and integration mutations exercised independent
paths, were restored byte-for-byte, and are absent from the final candidate.

The final commit only removes two redundant `anyhow` conversions exposed by
strict Clippy in test closures; no assertions or production behavior changed.
The full suite above requalifies both restored regressions on that final source.
[src: commit: 2d6fc6a46e5ba79d3bab661535040eb5f22448fb]

The real shell harness passed 282 tests after the UI helper stopped replacing
Bats' assertion failure helper. Actual failing-assertion controls and a disabled
disk guard were detected before exact restoration. Earlier apparently green
shell output with the shadowed helper is not evidence.
See [the harness diagnosis](../gotchas/bats-ui-failure-shadowing.md).

The frontend tree is unchanged from the 4,128-test /315-file full replay at
`23b8a867d09cbb7ab4bcdc38dddd8b3c05935538`. Both installed TypeScript
compilers, Oxlint, ESLint and i18n were checked again: zero lint errors,
63 existing ESLint warnings, and 4,486 keys in each of four locales. The pnpm
wrapper refused an automatic install through the shared node_modules symlink;
its guard was not bypassed. These checks used the already-installed binaries.
The isolated Python helper suite (795 tests) and sidecar suite (14) also passed.
See [the measured Rust profile tradeoff](0.13.0-rust-profile-benchmark.json).

## Remaining boundaries

This checkpoint does not finish KT-619's queued publication authority, approve
KT-637's separate statistics delivery, or qualify the whole-app browser/provider
matrix, desktop, fresh CI/coverage, migration, site screenshots or publication.
The historical remote DCO failure and missing sign-offs in the last two local
test commits still require reconciliation before publication; no history was
rewritten or force-pushed here. Existing ts-rs notices and the documented macOS
compact-unwind linker warning remain; a Clippy pass does not resolve them.
The shared plan, rather than this dated evidence record, owns live task status.
