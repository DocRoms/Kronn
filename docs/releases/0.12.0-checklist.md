# Kronn 0.12.0 release checklist

This file records release-candidate evidence. Do not mark an item complete from
an earlier branch state: every required gate must be rerun on the exact commit
that will be proposed as the 0.12.0 candidate.

## Candidate identity

- [x] Record the exact candidate commit after every 0.12.0 change and
      documentation update has been integrated.
- [x] Tested pre-compaction candidate SHA:
      `76107ec5d34a339d85383bb1daf953c6375faccf`; the DCO-safe six-commit history
      preserves its exact tree `a5c44a1de08ea6abc1a7753b0834dd4d63a589bb`.
- [x] Confirm `git status --short` is empty and the branch contains no
      unreviewed release-blocking task.

If a documentation-only evidence commit follows the tested candidate, record
both SHAs and rerun version, documentation, whitespace and hygiene gates on the
successor. Never present evidence from an older executable tree as current.

## Product and documentation

- [x] `CHANGELOG.md`, both READMEs, `docs/index.md`, the install guide and the
      FR/EN/ES public site describe the final 0.12.0 scope.
- [x] Named external API documentation covers LiteLLM, NVIDIA, OpenRouter and
      `Other`, including endpoint normalization, credential handling, gated
      model selection, named routing and migration behavior.
- [x] Quick Prompt documentation and release notes match searchable selectors,
      named-provider comparisons, copied run identifiers and provider/tier-safe
      reruns.
- [x] Project documentation and release notes match the Docker tab, Compose
      controls, published host and host-file diagnostics, logs, running badge
      and collection filter.
- [x] KT-481 (streamed/cancellable Ollama pulls) and KT-513 (bounded secure MCP
      bridge reload) are integrated and covered before their release claims.
- [x] Portable-library work and configurable documentation-path work remain
      outside the 0.12.0 claims.
- [x] Frontend packages and both Cargo lockfiles contain every compatible
      update available at candidate time; the unused `backoff` / `instant`
      chain is absent and the remaining `ttf-parser` maintenance advisory is
      documented as transitive through `pdf-extract` / `lopdf`.
- [x] `make check-version` passes and every hand-maintained third-party CLI
      version has been checked against its vendor registry.

## Migration safety

- [x] A fresh database applies migrations 1–153.
- [x] A representative 0.11.0 database upgrades through migrations 144–153
      while preserving projects, discussions, plans, task executions and Quick
      Prompts.
- [x] Legacy LiteLLM and NVIDIA settings become stable named connections
      without overwriting rows already edited through the named workflow.
- [x] Migration 152 repairs a database that already receipted the former
      three-preset schema, accepts `open_router` and preserves foreign-key
      integrity.
- [x] Migration 153 preserves Quick Prompts while adding their optional named
      external connection identity.
- [x] Automatic pre-migration database and configuration backups remain green.

## Candidate gates

Record exact commands, counts and conditional skips only after running them on
the candidate. The canonical entry points live in the Makefile and CI workflow.

- [x] `cargo fmt --all -- --check`.
- [x] Backend full test suite and integration tests.
- [x] Backend Clippy with warnings denied.
- [x] Repository-wide and key-management Rust coverage floors.
- [x] Backend policy lints, context budget, MCP surface budget and agent task
      matrix.
- [x] Python bridge and documentation-sidecar tests.
- [x] Shell integration tests.
- [x] Frontend unit and coverage suites, native/legacy typechecks, both linters,
      i18n parity and production build.
- [x] Playwright end-to-end suite; explain every conditional environment skip.
- [x] Desktop `cargo check --locked` or stronger build gate.
- [x] Regenerate TypeScript bindings and confirm the committed output is current.
- [x] Code-duplication ceiling, dependency audits and version sync.
- [x] Frontend and Cargo outdated checks report no remaining compatible update;
      incompatible major upgrades are evaluated separately rather than forced
      into the release candidate.
- [x] Documentation citations and relative Markdown links validate.
- [x] `git diff --check` and secret/artifact hygiene checks pass.

## Evidence on the final candidate

Complete this section only after the last executable change:

- Candidate identity: pre-compaction evidence SHA
  `76107ec5d34a339d85383bb1daf953c6375faccf`; immutable candidate tree
  `a5c44a1de08ea6abc1a7753b0834dd4d63a589bb`. The pull-request head containing
  this checklist carries the same tree after DCO-safe history compaction.
- Backend format / Clippy / tests / coverage: format and Clippy strict pass;
  6,212 tests pass with 5 explicitly ignored; coverage is 83.90% regions,
  83.63% functions and 83.42% lines against 83% floors. Targeted security and
  migration modules pass their raised 90–99% floors.
- Backend policy and budget gates: policy, context and agent-matrix gates pass;
  the MCP surface remains within budget at 106 tools and 87,762 bytes.
- Python bridge / sidecar / shell: 764 Python bridge tests, 14 documentation
  sidecar tests and 275 Bats tests pass.
- Frontend tests / coverage / typechecks / lint / i18n / build: 3,563 unit and
  coverage tests pass; coverage is 74.64% statements, 70.98% branches, 70.36%
  functions and 77.91% lines. Native and legacy typechecks, ESLint (0 errors,
  71 warnings within the 72-warning budget), oxlint, i18n parity and the
  production Vite build pass.
- Playwright results and conditional skips: 89 tests pass with no failure or
  flaky retry; 14 billed-provider canaries remain explicit opt-in skips.
- Desktop: `cargo check --locked` passes.
- Generated bindings / documentation links: `make typegen` and all 607 export
  tests pass; committed bindings are current; relative links and structured
  source citations validate.
- Version sync / whitespace / secret and artifact hygiene: version 0.12.0 is
  synchronized; `git diff --check`, credential-pattern and generated-artifact
  checks pass. The measured CI duplication perimeter is 2.62% against the new
  3% ceiling.
- Remaining non-blocking limitations: billed-provider canaries require explicit
  credentials and remain opt-in; the allowed `ttf-parser` maintenance advisory
  is transitive through `pdf-extract` / `lopdf`; desktop maintenance warnings
  remain constrained by the current Tauri/GTK stack. No release tag was made.

## Pull request and release boundary

- [ ] Push the exact candidate to `feat/0.12.0` and open or update its pull
      request against `main`.
- [ ] Apply the `ci-test` label and require every non-conditional CI job to pass.
- [ ] Correct every CI failure, refresh the candidate SHA and rerun affected
      local gates before declaring the pull request green.
- [ ] Tag or publish only with explicit maintainer authorization. Preparing and
      validating the pull request does not authorize a tag or release.
