- **ID**: TD-20260721-oxlint-migration
- **Area**: Frontend / Tooling / CI
- **Problem (fact)**: Kronn runs ESLint 10 with typescript-eslint, React Hooks and React Refresh plugins. CI allows at most 92 warnings, and the flat config contains project-specific selectors, severities, file overrides and React export exceptions that a replacement must preserve. [src: file: frontend/package.json:9-16] [src: file: frontend/eslint.config.js:6-135] [src: file: .github/workflows/ci-test.yml:343-352]
- **Resolution evidence**: A reviewed Oxlint configuration and `lint:fast` command now run before ESLint in CI. On the same machine Oxlint took 1.82 seconds and 207,142,912 bytes max RSS, versus 17.26 seconds and 1,323,696,128 bytes for ESLint. Oxlint reports 14 warnings while ESLint still reports 92, so ESLint remains authoritative. Full evidence is archived in `docs/operations/maintenance-0.9.3-baseline.md`.
- **Decision**: Ship the reversible dual-run for fast feedback, but reject replacement. The 78-diagnostic gap is chiefly React Compiler rules and `no-restricted-syntax`; removing ESLint would hide known debt. `no-unsafe-optional-chaining` is disabled only in Oxlint after five non-parity reports in tests, while ESLint continues to enforce project policy.
- **Impact**: Developers receive an approximately 89% faster early lint gate with 84% lower peak memory, while the full warning inventory and 92-warning ratchet remain intact.
- **Where (pointers)**: `frontend/package.json`, `frontend/pnpm-lock.yaml`, `frontend/eslint.config.js`, `.github/workflows/ci-test.yml`, and `docs/tech-debt/TD-20260713-typescript-7-native.md`.
- **Suggested direction (non-binding)**: Re-evaluate replacement only when the unsupported React Compiler and project-specific rules have explicit parity. Keep the native TypeScript compiler experiment independent.
- **Acceptance criteria**: Met for the dual-run: ESLint has zero errors, unsupported rules retain an explicit ESLint fallback, warning categories remain visible, full build/tests pass, measurements are recorded, and rollback is one CI step/script/config deletion.
- **Next step**: Reopen only after Oxlint closes the documented diagnostic gap.
- **Status**: RESOLVED — dual-run shipped; replacement rejected until parity exists.
